import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00C47C); // Hijau utama (tombol, icon)
  static const primaryLight = Color(0xFFE5FFF3); // Background card hijau soft
  static const secondary = Color(0xFF30C3FC); // Biru muda (opsional)
  static const danger = Color(0xFFFF5C5C); // Untuk warning / error

  static const background = Colors.white;
  static const card = Color.fromARGB(255, 236, 248, 242);
  final cardBorder = Color(0xFF1FCC79).withOpacity(1); // Border hijau soft

  static const mainText = Color(0xFF2E3E5C);
  static const darkText = Color(0xFF222222);
  static const lightText = Color(0xFF777777);
  static const secondaryText = Color(0xFF9FA5C0);

  static const shadow = Colors.black12;
  static const outline = Color(0xFFD0DBEA);
}
